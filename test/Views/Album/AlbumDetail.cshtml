@model MusicApp.ViewModels.AlbumDetailPageViewModel

<!-- ##### Featured Artist Area Start ##### -->
<section class="featured-artist-area section-padding-100 bg-img bg-overlay bg-fixed" style="background-image: url(../../Content/img/bg-img/bg-4.jpg);">
    <div class="container">
        <div class="row align-items-end">
            <div class="col-12 col-md-5 col-lg-4">
                <div class="featured-artist-thumb">
                    <img src="@Model.AlbumDetail.CoverImageUrl" alt="@Model.AlbumDetail.Title">
                </div>
            </div>
            <div class="col-12 col-md-7 col-lg-8">
                <div class="featured-artist-content">
                    <!-- Section Heading -->
                    <div class="section-heading white text-left mb-30">
                        <p>See what’s new</p>
                        <h2>@Model.AlbumDetail.Title</h2>
                    </div>
                    <p>Release Date: @(Model.AlbumDetail.ReleaseDate?.ToString("dd/MM/yyyy") ?? "N/A")</p>
                    <p>Artist: @Model.AlbumDetail.Artist</p>

                    <h4>Songs:</h4>
                    @if (Model.AlbumDetail.Songs != null && Model.AlbumDetail.Songs.Any())
                    {
                        foreach (var song in Model.AlbumDetail.Songs)
                        {
                            <div class="song-play-area">
                                <div class="song-name">
                                    <p>@song.Title</p>
                                </div>
                                <audio preload="auto" controls>
                                    <source src="@song.Url">
                                </audio>
                            </div>
                        }
                    }
                    else
                    {
                        <p>No songs available for this album.</p>
                    }
                </div>
            </div>
        </div>
    </div>
</section>
<!-- ##### Featured Artist Area End ##### -->
